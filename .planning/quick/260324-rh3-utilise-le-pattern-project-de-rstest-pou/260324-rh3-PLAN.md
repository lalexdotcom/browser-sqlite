---
quick_id: 260324-rh3
type: execute
wave: 1
depends_on: []
files_modified:
  - rstest.config.ts
  - rstest.browser.config.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "`rstest` (sans argument) lance les tests unit ET browser"
    - "`pnpm test` passe sans erreur"
    - "`rstest.browser.config.ts` n'existe plus"
  artifacts:
    - path: "rstest.config.ts"
      provides: "Config unifiée avec projects array (unit + browser)"
      contains: "projects"
    - path: "package.json"
      provides: "Scripts mis à jour"
      contains: "rstest --project browser"
  key_links:
    - from: "rstest.config.ts"
      to: "tests/browser/**/*.test.ts"
      via: "project browser include"
    - from: "rstest.config.ts"
      to: "tests/**/*.test.ts"
      via: "project unit include"
---

<objective>
Fusionner rstest.config.ts et rstest.browser.config.ts en un seul fichier
en utilisant le pattern `projects` de rstest/Vitest, de sorte que `rstest`
lance les deux suites (unit Node + browser Playwright/Chromium) en une
seule commande.

Purpose: Simplifier le workflow de test — une seule commande couvre tout.
Output: rstest.config.ts unifié, rstest.browser.config.ts supprimé,
        package.json mis à jour.
</objective>

<execution_context>
@/workspaces/wsqlite/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/workspaces/wsqlite/.planning/STATE.md
@/workspaces/wsqlite/rstest.config.ts
@/workspaces/wsqlite/rstest.browser.config.ts
@/workspaces/wsqlite/package.json

<interfaces>
<!-- État actuel des deux configs -->

rstest.config.ts (unit, Node):
- extends: withRslibConfig()
- include: ['tests/**/*.test.ts', 'tests/**/*.spec.ts']
- exclude: ['tests/browser/**']
- passWithNoTests: true

rstest.browser.config.ts (browser, Playwright):
- extends: withRslibConfig()
- pluginCrossOriginIsolation: injecte COOP/COEP headers (requis pour SharedArrayBuffer)
- browser: { enabled: true, provider: 'playwright', browser: 'chromium', headless: true }
- plugins: [pluginCrossOriginIsolation]
- include: ['tests/browser/**/*.test.ts']
- exclude: ['**/worktrees/**']
- testTimeout: 30000
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fusionner les configs avec le pattern projects</name>
  <files>rstest.config.ts, rstest.browser.config.ts</files>
  <action>
Réécrire rstest.config.ts avec le pattern `projects`. La nouvelle config doit :

1. Conserver les imports existants : `withRslibConfig` et `defineConfig`
2. Déplacer `pluginCrossOriginIsolation` depuis rstest.browser.config.ts
   dans rstest.config.ts (en tête de fichier, avant l'export default)
3. Utiliser `extends: withRslibConfig()` au niveau racine (comme avant)
4. Ajouter un tableau `projects` avec deux entrées :

   Projet "unit" :
   - name: 'unit'
   - include: ['tests/**/*.test.ts', 'tests/**/*.spec.ts']
   - exclude: ['tests/browser/**']
   - passWithNoTests: true

   Projet "browser" :
   - name: 'browser'
   - browser: { enabled: true, provider: 'playwright', browser: 'chromium', headless: true }
   - plugins: [pluginCrossOriginIsolation]
   - include: ['tests/browser/**/*.test.ts']
   - exclude: ['**/worktrees/**']
   - testTimeout: 30000

Après avoir écrit rstest.config.ts, supprimer rstest.browser.config.ts
avec la commande : `rm /workspaces/wsqlite/rstest.browser.config.ts`
  </action>
  <verify>
    <automated>test -f /workspaces/wsqlite/rstest.config.ts && ! test -f /workspaces/wsqlite/rstest.browser.config.ts && grep -q 'projects' /workspaces/wsqlite/rstest.config.ts && grep -q 'pluginCrossOriginIsolation' /workspaces/wsqlite/rstest.config.ts && echo OK</automated>
  </verify>
  <done>
    rstest.config.ts contient le tableau projects avec les deux entrées (unit +
    browser), pluginCrossOriginIsolation est défini dans ce fichier,
    rstest.browser.config.ts n'existe plus.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mettre à jour package.json et valider</name>
  <files>package.json</files>
  <action>
Mettre à jour le script `test:browser` dans package.json :
- Avant : `"test:browser": "rstest --config rstest.browser.config.ts"`
- Après : `"test:browser": "rstest --project browser"`

Le script `test` reste `"rstest"` (inchangé).

Ensuite, valider que les tests passent :
  `cd /workspaces/wsqlite && pnpm test`

Si les tests échouent à cause de la config projects, vérifier :
- Que `extends` est bien au niveau racine (pas dans chaque project)
- Que les chemins include/exclude sont corrects
- Que le type de `projects` est accepté par la version de @rstest/core installée
  (^0.9.0 — même API que Vitest projects)
  </action>
  <verify>
    <automated>cd /workspaces/wsqlite && grep '"test:browser"' package.json | grep -q 'rstest --project browser' && echo "package.json OK" && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    package.json mis à jour, `pnpm test` passe sans erreur (les tests unit
    s'exécutent avec succès via le projet "unit").
  </done>
</task>

</tasks>

<verification>
- `rstest.config.ts` contient `projects` avec deux entrées nommées `unit` et `browser`
- `rstest.browser.config.ts` n'existe plus
- `pnpm test` passe (tests unit)
- `package.json` : `test:browser` pointe sur `rstest --project browser`
- `grep -q 'pluginCrossOriginIsolation' rstest.config.ts` retourne 0
</verification>

<success_criteria>
Une seule commande `rstest` (ou `pnpm test`) lance les deux suites.
rstest.browser.config.ts supprimé. package.json cohérent avec la nouvelle config.
</success_criteria>

<output>
Après completion, créer `.planning/quick/260324-rh3-utilise-le-pattern-project-de-rstest-pou/260324-rh3-SUMMARY.md`
avec un résumé des changements effectués.
</output>
