---
phase: quick
plan: 260324-o2g
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.ts
  - src/types.ts
  - src/worker.ts
  - src/client.ts
  - package.json
  - tests/unit/logger.test.ts
  - README.md
autonomous: true
requirements: [QUICK-o2g]
must_haves:
  truths:
    - "Le code source ne contient plus aucune référence à @lalex/console"
    - "L'option logLevel n'existe plus dans CreateSQLiteClientOptions ni dans ClientMessageData"
    - "worker.ts ne logue plus rien (pas de log(), pas de currentLogLevel)"
    - "client.ts ne dispatche plus les messages de type 'log' depuis les workers"
    - "Les tests unitaires passent sans erreur de compilation ni d'exécution"
  artifacts:
    - path: src/logger.ts
      provides: "Fichier supprimé ou vidé (plus aucune logique de logging)"
    - path: src/types.ts
      provides: "Types sans LogLevel, sans logLevel dans ClientMessageData, sans variante log dans WorkerMessageData"
    - path: src/worker.ts
      provides: "Worker sans import shouldLog, sans currentLogLevel, sans fonction log()"
    - path: src/client.ts
      provides: "Client sans import LL, sans logLevel option, sans handler type:'log'"
    - path: tests/unit/logger.test.ts
      provides: "Fichier supprimé (les symboles testés n'existent plus)"
  key_links:
    - from: src/client.ts
      to: src/worker.ts
      via: "postMessage open — le champ logLevel doit être absent"
    - from: src/worker.ts
      to: src/client.ts
      via: "postMessage type:'log' — doit ne plus jamais être envoyé"
---

<objective>
Supprimer toute la couche de logging de wsqlite : @lalex/console (dépendance optionnelle),
l'option logLevel, le type LogLevel, le fichier logger.ts, et tous les appels log() dans worker.ts.
Les workers cessent d'émettre des messages de type 'log'. Le client cesse de les router.

Purpose: Simplifier le code en éliminant le logging interne. Un mécanisme de tracing sera
repensé ultérieurement.
Output: Code source propre sans aucune référence au logging. Tests et build verts.
</objective>

<execution_context>
@/workspaces/wsqlite/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/wsqlite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supprimer logger.ts, nettoyer types.ts et package.json</name>
  <files>src/logger.ts, src/types.ts, package.json, tests/unit/logger.test.ts</files>
  <action>
**src/logger.ts** — Supprimer le fichier entièrement (delete).

**tests/unit/logger.test.ts** — Supprimer le fichier entièrement (delete). Il teste
`shouldLog` et `makeConsoleShim` qui n'existent plus.

**src/types.ts** — Effectuer trois suppressions chirurgicales :
1. Supprimer le champ `logLevel?: 'debug' | 'info' | 'warn' | 'error';` de la variante
   `type: 'open'` dans `ClientMessageData` (ligne 44).
2. Supprimer le type `LogLevel` en entier (lignes 48-59).
3. Supprimer la variante log dans `WorkerMessageData` :
   `| { type: 'log'; level: LogLevel; scope: string; args: unknown[] };` (ligne 66).

**package.json** — Supprimer la section `"optionalDependencies"` en entier (elle ne contient
que `@lalex/console`).
  </action>
  <verify>
    <automated>cd /workspaces/wsqlite && [ ! -f src/logger.ts ] && [ ! -f tests/unit/logger.test.ts ] && ! grep -q "LogLevel\|logLevel\|@lalex/console\|type: 'log'" src/types.ts package.json && echo "OK"</automated>
  </verify>
  <done>
logger.ts supprimé, logger.test.ts supprimé, LogLevel/logLevel absents de types.ts,
@lalex/console absent de package.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Nettoyer worker.ts et client.ts</name>
  <files>src/worker.ts, src/client.ts</files>
  <action>
**src/worker.ts** — Quatre suppressions :
1. Ligne 12 : supprimer `import { shouldLog } from './logger';`
2. Ligne 16 : dans l'import depuis `'./types'`, retirer `LogLevel` de la liste des types importés.
3. Lignes 20-31 : supprimer `let currentLogLevel: LogLevel = 'warn';` et la fonction `log()` complète.
4. Supprimer TOUS les appels `log(...)` dans le corps du fichier (environ 15 appels dispersés dans `open()` et le handler query).
5. Lignes 314-315 dans le handler `self.onmessage` : supprimer `const { file, flags, index, vfs, pragmas, logLevel } = data;` → remplacer par `const { file, flags, index, vfs, pragmas } = data;`, et supprimer la ligne `currentLogLevel = logLevel ?? 'warn';`.

**src/client.ts** — Quatre suppressions :
1. Ligne 1 : supprimer `import { LL } from './logger';`
2. Dans `CreateSQLiteClientOptions` : supprimer le champ JSDoc + la propriété `logLevel?: 'debug' | 'info' | 'warn' | 'error';` (lignes 56-61).
3. Dans `createSQLiteClient` : supprimer la ligne `LL.level = clientOptions?.logLevel ?? 'warn';` (ligne 297).
4. Dans `worker.onmessage` (handler dans `createWorker`) : supprimer le bloc entier qui gère `data.type === 'log'` (lignes 368-374 — le bloc `if (data.type === 'log') { ... return; }`).
5. Supprimer tous les appels `LL.*()` dans le fichier : `LL.debug(...)`, `LL.verb(...)`, `LL.success(...)`, `LL.error(...)` (lignes ~537, ~578, ~585, ~599, ~1027, ~1029).
6. Dans `worker.postMessage({ type: 'open', ... })` : retirer le champ `logLevel: clientOptions?.logLevel,` (ligne ~490).

Note : `LL` n'est utilisé nulle part ailleurs après ces suppressions — vérifier avec grep.
  </action>
  <verify>
    <automated>cd /workspaces/wsqlite && ! grep -q "shouldLog\|currentLogLevel\|\bLL\b\|from './logger'\|logLevel\|type: 'log'" src/worker.ts src/client.ts && echo "SOURCES_CLEAN" && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
worker.ts et client.ts ne contiennent plus aucune référence au logging.
`pnpm run build` se termine sans erreur TypeScript.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mettre à jour README.md et valider les tests</name>
  <files>README.md</files>
  <action>
**README.md** — Supprimer la ligne `logLevel: 'warn',               // log verbosity (default: 'warn')` de l'exemple de code dans la section Quick Start (ligne 84). Vérifier qu'aucune autre mention de `logLevel` ou `@lalex/console` ne subsiste dans le fichier.

Ensuite valider l'ensemble :
- `pnpm test` (tests unitaires rstest) doit passer.
- `pnpm run build` doit produire le dist sans erreurs.
  </action>
  <verify>
    <automated>cd /workspaces/wsqlite && ! grep -q "logLevel\|@lalex/console" README.md && echo "README_CLEAN" && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>
README.md ne mentionne plus logLevel. `pnpm test` passe (logger.test.ts supprimé,
debug.test.ts et les tests restants sont verts). Build propre.
  </done>
</task>

</tasks>

<verification>
```bash
cd /workspaces/wsqlite
# Aucune référence résiduelle
grep -rn "@lalex/console\|logLevel\|shouldLog\|currentLogLevel\|makeConsoleShim\|ScopedLogger\|from './logger'" src/ tests/ README.md package.json 2>/dev/null && echo "RESIDUES FOUND" || echo "ALL CLEAN"
# Build propre
pnpm run build
# Tests verts
pnpm test
```
</verification>

<success_criteria>
- `src/logger.ts` n'existe plus
- `tests/unit/logger.test.ts` n'existe plus
- `grep -r "@lalex/console\|logLevel\|shouldLog" src/ tests/ README.md package.json` — aucun résultat
- `pnpm run build` — exit 0, pas d'erreur TypeScript
- `pnpm test` — exit 0, tous les tests passent
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260324-o2g-enleve-toute-r-f-rence-lalex-console-et-/260324-o2g-SUMMARY.md`
</output>
