---
phase: 01-bug-fixes-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types.ts
autonomous: true
requirements:
  - BUG-04
  - BUG-05

must_haves:
  truths:
    - "npm install resolves @lalex/promises without error"
    - "The open message sent from client to worker carries a logLevel field"
    - "TypeScript accepts the logLevel field as part of ClientMessageData"
  artifacts:
    - path: "package.json"
      provides: "@lalex/promises in dependencies section"
      contains: '"@lalex/promises"'
    - path: "src/types.ts"
      provides: "logLevel field on the open message variant of ClientMessageData"
      exports: ["ClientMessageData"]
  key_links:
    - from: "src/client.ts"
      to: "src/types.ts ClientMessageData open variant"
      via: "postMessage call at line 249-257 (worker.postMessage open payload)"
      pattern: "logLevel.*ClientMessageData|ClientMessageData.*logLevel"
    - from: "src/worker.ts"
      to: "src/types.ts ClientMessageData open variant"
      via: "event.data destructuring in self.onmessage open handler"
      pattern: "logLevel.*data\\.type.*open|const.*logLevel.*data"
---

<objective>
Add `@lalex/promises` to `package.json` dependencies (BUG-04) and extend the `ClientMessageData` open message type in `src/types.ts` to carry a `logLevel` field (BUG-05 prerequisite). These are the shared foundation changes that Wave 2 plans depend on.

Purpose: Fix the missing dependency so any file importing `client.ts` can be installed and tested. Establish the typed contract for passing `logLevel` from client to worker before both files are updated in Wave 2.

Output: Updated `package.json` with `@lalex/promises` in `dependencies`; updated `src/types.ts` where `ClientMessageData` open variant includes `logLevel?: 'debug' | 'info' | 'warn' | 'error'`.
</objective>

<execution_context>
@/workspaces/wsqlite/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/wsqlite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bug-fixes-type-safety/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @lalex/promises to package.json dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json — read the current dependencies section before modifying; verify @lalex/promises is absent
  </read_first>
  <action>
    Open `package.json`. In the `"dependencies"` object (currently contains only `@lalex/console` and `wa-sqlite`), add:

    ```json
    "@lalex/promises": "*"
    ```

    The resulting `dependencies` block must look like:

    ```json
    "dependencies": {
      "@lalex/console": "2.0.0-rc.1",
      "@lalex/promises": "*",
      "wa-sqlite": "github:rhashimoto/wa-sqlite#v1.0.9"
    }
    ```

    After saving, run `npm install` to confirm the package resolves without error. Do NOT use `--legacy-peer-deps` or any flags that suppress errors — the install must succeed cleanly.
  </action>
  <verify>
    <automated>grep -F '"@lalex/promises"' /workspaces/wsqlite/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the line `"@lalex/promises": "*"` inside the `dependencies` object (not `devDependencies`)
    - `npm install` exits with code 0
    - `node_modules/@lalex/promises` directory exists after install
  </acceptance_criteria>
  <done>`@lalex/promises` is a declared dependency and resolves on install</done>
</task>

<task type="auto">
  <name>Task 2: Add logLevel field to ClientMessageData open variant in types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts — read the full file; the `ClientMessageData` type is defined here with an `open` variant (lines 36-45 at time of planning)
  </read_first>
  <action>
    Open `src/types.ts`. Find the `ClientMessageData` union type. Its `open` variant currently looks like:

    ```typescript
    | {
        type: 'open';
        file: string;
        flags: SharedArrayBuffer;
        index: number;
        vfs?: SQLiteVFS;
        pragmas?: Record<string, string>;
      }
    ```

    Add one optional field `logLevel` after `pragmas`:

    ```typescript
    | {
        type: 'open';
        file: string;
        flags: SharedArrayBuffer;
        index: number;
        vfs?: SQLiteVFS;
        pragmas?: Record<string, string>;
        logLevel?: 'debug' | 'info' | 'warn' | 'error';
      }
    ```

    No other changes to `types.ts`.
  </action>
  <verify>
    <automated>grep -F "logLevel" /workspaces/wsqlite/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/types.ts` contains `logLevel?: 'debug' | 'info' | 'warn' | 'error';` inside the `open` variant of `ClientMessageData`
    - The union type still compiles — no other fields were removed or renamed
    - `grep -c "logLevel" /workspaces/wsqlite/src/types.ts` outputs `1` (exactly one occurrence — only the field declaration, not a stray duplicate)
  </acceptance_criteria>
  <done>The open message contract carries logLevel, ready for Wave 2 plans to read it in both client.ts and worker.ts</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep '"@lalex/promises"' package.json` — must match
2. `grep 'logLevel' src/types.ts` — must match
3. `npm install` exits 0
4. `npx tsc --noEmit` (or project's type-check command) on `src/types.ts` produces no errors for the types.ts changes
</verification>

<success_criteria>
- `package.json` dependencies includes `"@lalex/promises": "*"`
- `src/types.ts` `ClientMessageData` open variant has `logLevel?: 'debug' | 'info' | 'warn' | 'error'`
- `npm install` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-type-safety/01-01-SUMMARY.md`
</output>
