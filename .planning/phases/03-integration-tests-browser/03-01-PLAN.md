---
phase: 03-integration-tests-browser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - rstest.browser.config.ts
  - tests/browser/helpers.ts
autonomous: true
requirements:
  - INT-01

must_haves:
  truths:
    - "pnpm test:browser lance Rstest en mode browser sur Chromium headless sans erreur de configuration"
    - "`self.crossOriginIsolated` est `true` dans le contexte browser — COOP/COEP headers bien servis"
    - "`createTestClient()` retourne un `SQLiteDB` avec nettoyage OPFS automatique après chaque test"
  artifacts:
    - path: "rstest.browser.config.ts"
      provides: "Browser Rstest config with Playwright + COOP/COEP headers"
      contains: "browser: { enabled: true, provider: 'playwright', browser: 'chromium' }"
    - path: "tests/browser/helpers.ts"
      provides: "createTestClient() helper with UUID-based DB name and afterEach OPFS cleanup"
      exports: ["createTestClient"]
    - path: "package.json"
      provides: "test:browser script and @rstest/browser + playwright devDependencies"
      contains: "test:browser"
  key_links:
    - from: "rstest.browser.config.ts"
      to: "Rsbuild dev server"
      via: "server.headers in defineConfig"
      pattern: "Cross-Origin-Opener-Policy.*same-origin"
    - from: "tests/browser/helpers.ts"
      to: "src/client.ts"
      via: "import { createSQLiteClient }"
      pattern: "createSQLiteClient"
---

<objective>
Mettre en place toute l'infrastructure nécessaire pour exécuter des tests d'intégration dans un vrai navigateur Chromium headless via Rstest browser mode.

Purpose: Sans cette infrastructure, aucun test d'intégration browser ne peut tourner. Les tests des plans 02 et 03 dépendent tous de cette base.
Output: `rstest.browser.config.ts` avec Playwright + COOP/COEP, `tests/browser/helpers.ts` avec isolation OPFS, `package.json` mis à jour avec devDependencies et script `test:browser`.
</objective>

<execution_context>
@/workspaces/wsqlite/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/wsqlite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-integration-tests-browser/03-CONTEXT.md
@.planning/phases/03-integration-tests-browser/03-RESEARCH.md

<interfaces>
<!-- Contrats extraits du codebase. L'exécuteur utilise ces signatures directement. -->

From rstest.config.ts (config Node-mode à ne PAS modifier, à utiliser comme référence de pattern):
```typescript
import { withRslibConfig } from '@rstest/adapter-rslib';
import { defineConfig } from '@rstest/core';

export default defineConfig({
  extends: withRslibConfig(),
  include: ['tests/**/*.test.ts', 'tests/**/*.spec.ts'],
  passWithNoTests: true,
});
```

From src/client.ts:
```typescript
export type CreateSQLiteClientOptions = {
  name?: string;
  poolSize?: number;
  vfs?: SQLiteVFS;
  pragmas?: Record<string, string>;
  logLevel?: 'debug' | 'info' | 'warn' | 'error';
};

export const createSQLiteClient = (
  file: string,
  clientOptions?: CreateSQLiteClientOptions,
) => { /* ... */ };
// Retourne : { read, write, stream, one, transaction, bulkWrite, output, close, debug }
// createSQLiteClient est synchrone — les workers s'initialisent en arrière-plan
// La première requête attend automatiquement que les workers soient READY
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer @rstest/browser et playwright</name>
  <files>package.json</files>
  <read_first>
    - package.json — voir les devDependencies existants et les scripts actuels
  </read_first>
  <action>
    Exécuter la commande d'installation :
    ```
    pnpm add -D @rstest/browser@0.9.4 playwright@1.58.2
    ```

    Ensuite ajouter le script `test:browser` dans la section `scripts` de package.json :
    ```json
    "test:browser": "rstest --config rstest.browser.config.ts"
    ```

    Le script `test` existant (`"test": "rstest"`) doit rester inchangé — il cible le mode Node.

    Note : `@rstest/browser` télécharge automatiquement Chromium lors de la première exécution via son intégration Playwright. Aucun `playwright install` séparé n'est requis pour ce chemin d'intégration.
  </action>
  <verify>
    <automated>node -e "const p = require('/workspaces/wsqlite/package.json'); if (!p.devDependencies['@rstest/browser']) throw new Error('missing @rstest/browser'); if (!p.devDependencies['playwright']) throw new Error('missing playwright'); if (!p.scripts['test:browser']) throw new Error('missing test:browser script'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contient `"@rstest/browser": "0.9.4"` dans devDependencies
    - package.json contient `"playwright": "1.58.2"` dans devDependencies
    - package.json contient `"test:browser": "rstest --config rstest.browser.config.ts"` dans scripts
    - `pnpm install` se termine sans erreur
  </acceptance_criteria>
  <done>Les deux packages sont installés et le script test:browser est disponible</done>
</task>

<task type="auto">
  <name>Task 2: Créer rstest.browser.config.ts avec Playwright et COOP/COEP</name>
  <files>rstest.browser.config.ts</files>
  <read_first>
    - rstest.config.ts — pattern de référence (`withRslibConfig` + `defineConfig`)
    - .planning/phases/03-integration-tests-browser/03-RESEARCH.md §Pattern 1 — config browser avec COOP/COEP
  </read_first>
  <action>
    Créer le fichier `rstest.browser.config.ts` à la racine du projet avec ce contenu exact :

    ```typescript
    import { withRslibConfig } from '@rstest/adapter-rslib';
    import { defineConfig } from '@rstest/core';

    export default defineConfig({
      extends: withRslibConfig(),
      browser: {
        enabled: true,
        provider: 'playwright',
        browser: 'chromium',
        headless: true,
      },
      // COOP/COEP requis pour SharedArrayBuffer (WorkerOrchestrator)
      // D-04 : headers injectés via Rsbuild server.headers
      server: {
        headers: {
          'Cross-Origin-Opener-Policy': 'same-origin',
          'Cross-Origin-Embedder-Policy': 'require-corp',
        },
      },
      include: ['tests/browser/**/*.test.ts'],
      testTimeout: 30000,
    });
    ```

    Si TypeScript rejette la clé `server` au niveau racine (erreur de type `RstestConfig`), utiliser
    le fallback via `withRslibConfig` :

    ```typescript
    import { withRslibConfig } from '@rstest/adapter-rslib';
    import { defineConfig } from '@rstest/core';

    export default defineConfig({
      extends: withRslibConfig({
        modifyLibConfig: (config: any) => {
          config.server = {
            ...config.server,
            headers: {
              'Cross-Origin-Opener-Policy': 'same-origin',
              'Cross-Origin-Embedder-Policy': 'require-corp',
            },
          };
          return config;
        },
      }),
      browser: {
        enabled: true,
        provider: 'playwright',
        browser: 'chromium',
        headless: true,
      },
      include: ['tests/browser/**/*.test.ts'],
      testTimeout: 30000,
    });
    ```

    Essayer la forme directe d'abord. Choisir la forme qui compile sans erreur TypeScript.
    Vérifier avec `pnpm exec tsc --noEmit rstest.browser.config.ts` ou en lançant `pnpm test:browser` (même si aucun test n'existe encore, le chargement de config doit réussir).
  </action>
  <verify>
    <automated>test -f /workspaces/wsqlite/rstest.browser.config.ts && grep -q "provider.*playwright" /workspaces/wsqlite/rstest.browser.config.ts && grep -q "Cross-Origin-Opener-Policy" /workspaces/wsqlite/rstest.browser.config.ts && grep -q "Cross-Origin-Embedder-Policy" /workspaces/wsqlite/rstest.browser.config.ts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - rstest.browser.config.ts existe à la racine du projet
    - Contient `provider: 'playwright'`
    - Contient `browser: 'chromium'`
    - Contient `headless: true`
    - Contient `'Cross-Origin-Opener-Policy': 'same-origin'`
    - Contient `'Cross-Origin-Embedder-Policy': 'require-corp'`
    - Contient `include: ['tests/browser/**/*.test.ts']`
    - Contient `testTimeout: 30000`
    - `pnpm build` ou `pnpm exec tsc --noEmit` ne produit pas d'erreur TypeScript sur ce fichier
  </acceptance_criteria>
  <done>Config browser valide — Playwright Chromium headless avec COOP/COEP configurés</done>
</task>

<task type="auto">
  <name>Task 3: Créer tests/browser/helpers.ts avec createTestClient()</name>
  <files>tests/browser/helpers.ts</files>
  <read_first>
    - src/client.ts — signature de `createSQLiteClient(file, options?)` et type `SQLiteDB`
    - .planning/phases/03-integration-tests-browser/03-RESEARCH.md §Pattern 2 — helper OPFS cleanup
    - .planning/phases/03-integration-tests-browser/03-CONTEXT.md §D-06, D-07, D-08
  </read_first>
  <action>
    Créer le répertoire `tests/browser/` s'il n'existe pas, puis créer `tests/browser/helpers.ts` :

    ```typescript
    import { afterEach } from '@rstest/core';
    import { createSQLiteClient } from '../../src/client';

    /**
     * Crée un client SQLite avec un nom de base unique (UUID) et enregistre
     * un nettoyage OPFS automatique via afterEach.
     *
     * Décisions : D-06 (nom unique), D-07 (afterEach cleanup), D-08 (helper partagé)
     * VFS : OPFSPermutedVFS par défaut (D-05) — ne pas passer d'option `vfs`
     */
    export async function createTestClient() {
      const dbName = `wsqlite-test-${crypto.randomUUID()}`;

      afterEach(async () => {
        try {
          const root = await navigator.storage.getDirectory();
          await root.removeEntry(dbName, { recursive: true });
        } catch {
          // L'entrée OPFS peut ne pas exister si le test a échoué avant la création de la DB
        }
      });

      // createSQLiteClient est synchrone — workers s'initialisent en arrière-plan.
      // La première requête queue jusqu'à ce qu'un worker soit READY.
      return createSQLiteClient(dbName);
    }
    ```

    Points critiques :
    - Utiliser `crypto.randomUUID()` (natif browser, pas d'import) — pas `Date.now()` (risque collision parallèle)
    - Ne PAS passer `vfs` — laisser le défaut `OPFSPermutedVFS` s'appliquer (D-05)
    - `afterEach` est importé de `@rstest/core` — ne pas utiliser `vitest` ou autre runner
    - Le `afterEach` doit être enregistré AVANT le retour du client, pour garantir le cleanup même si le test throw
  </action>
  <verify>
    <automated>test -f /workspaces/wsqlite/tests/browser/helpers.ts && grep -q "createTestClient" /workspaces/wsqlite/tests/browser/helpers.ts && grep -q "crypto.randomUUID" /workspaces/wsqlite/tests/browser/helpers.ts && grep -q "removeEntry" /workspaces/wsqlite/tests/browser/helpers.ts && grep -q "afterEach" /workspaces/wsqlite/tests/browser/helpers.ts && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - tests/browser/helpers.ts existe
    - Exporte la fonction `createTestClient`
    - Utilise `crypto.randomUUID()` pour le nom unique (préfixe `wsqlite-test-`)
    - Importe `afterEach` depuis `@rstest/core`
    - Appelle `navigator.storage.getDirectory()` puis `removeEntry(dbName, { recursive: true })` dans afterEach
    - Importe `createSQLiteClient` depuis `../../src/client`
    - N'utilise PAS l'option `vfs` (laisse le défaut OPFSPermutedVFS)
    - Le try/catch autour du removeEntry empêche les erreurs si la DB n'a pas été créée
  </acceptance_criteria>
  <done>Helper createTestClient() disponible avec isolation OPFS garantie par afterEach</done>
</task>

</tasks>

<verification>
Infrastructure complète quand :
1. `grep -q '"@rstest/browser"' package.json` — package installé
2. `grep -q '"playwright"' package.json` — peer dep installé
3. `grep -q 'test:browser' package.json` — script disponible
4. `test -f rstest.browser.config.ts` — config browser existe
5. `grep -q 'Cross-Origin-Opener-Policy' rstest.browser.config.ts` — COOP header configuré
6. `test -f tests/browser/helpers.ts` — helper existe
7. `grep -q 'createTestClient' tests/browser/helpers.ts` — fonction exportée
</verification>

<success_criteria>
- `@rstest/browser` et `playwright` ajoutés aux devDependencies
- `pnpm test:browser` est une commande valide (ne crash pas immédiatement sur la config)
- `rstest.browser.config.ts` configure Playwright Chromium headless avec COOP/COEP
- `tests/browser/helpers.ts` fournit `createTestClient()` avec isolation OPFS
- Les plans 02 et 03 peuvent être exécutés en parallèle (fichiers non conflictuels)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-integration-tests-browser/03-01-SUMMARY.md`
</output>
